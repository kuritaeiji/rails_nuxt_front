setup:
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
build:
  docker:
    web: Dockerfile
  config:
    API_URL: 'https://rails-nuxt-back.herokuapp.com'
run:
  web: yarn run start
